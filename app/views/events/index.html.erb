
Past Events
<ul>
    <% @past_events.each do |event| %>
        <li><%= event.name %>: <%= event.event_date %>  <%= link_to 'See event', event_path(event) %></li>
    <% end %>
</ul>

Future Events
<ul>
        <% @future_events.each do |event| %>
            <li><%= event.name %>: <%= event.event_date %>  <%= link_to 'See event', event_path(event) %></li>
        <% end %>
</ul>

